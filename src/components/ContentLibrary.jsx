import React, { useEffect, useMemo, useState } from 'react';
import { firebaseApi } from '@/api/firebaseClient';
import { blankFormula, blankQuestion, normalizeItem, validateItem } from '@/lib/importEngine';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Textarea } from '@/components/ui/textarea';
import { Label } from '@/components/ui/label';
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Tabs, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { useToast } from '@/components/ui/use-toast';
import { Loader2, Pencil, Plus, Search, Sparkles, Trash2 } from 'lucide-react';
import ImportExport from '@/components/flashcards/ImportExport';

const formulaFields = [['subject', 'Subject'], ['topic', 'Topic'], ['subtopic', 'Subtopic'], ['name', 'Formula name'], ['formula', 'Formula (LaTeX supported)'], ['description', 'Description'], ['variables', 'Variables'], ['units', 'Units'], ['conditions', 'Conditions'], ['applications', 'Applications'], ['exampleProblem', 'Example problem'], ['solution', 'Complete solution'], ['finalAnswer', 'Final answer'], ['commonMistakes', 'Common mistakes'], ['relatedFormulas', 'Related formulas'], ['references', 'References'], ['tags', 'Tags (comma separated)']];
const questionFields = [['subject', 'Subject'], ['topic', 'Topic'], ['difficulty', 'Difficulty'], ['question', 'Question'], ['choices', 'Choices A-D (separate with |)'], ['correctAnswer', 'Correct answer'], ['explanation', 'Explanation'], ['formulaReferences', 'Formula references'], ['tags', 'Tags (comma separated)'], ['boardYear', 'Board year'], ['source', 'Source']];

export default function ContentLibrary({ type }) {
  const isFormula = type === 'formula'; const Entity = isFormula ? firebaseApi.entities.Formula : firebaseApi.entities.Question; const singular = isFormula ? 'Formula' : 'Question'; const makeBlank = isFormula ? blankFormula : blankQuestion; const fields = isFormula ? formulaFields : questionFields;
  const [user, setUser] = useState(null); const [items, setItems] = useState([]); const [draft, setDraft] = useState(makeBlank()); const [editing, setEditing] = useState(null); const [mode, setMode] = useState('manual'); const [open, setOpen] = useState(false); const [search, setSearch] = useState(''); const [busy, setBusy] = useState(false); const [preview, setPreview] = useState([]); const [ai, setAi] = useState({ subject: '', topic: '', difficulty: 'medium', count: 5 }); const { toast } = useToast();
  useEffect(() => { (async () => { const me = await firebaseApi.auth.me(); setUser(me); setItems(await Entity.filter({ user_id: me.id })); })(); }, [type]);
  const filtered = useMemo(() => { const q = search.toLowerCase(); return items.filter((item) => !q || [item.name, item.subject, item.topic, item.question, item.tags, item.source].join(' ').toLowerCase().includes(q)); }, [items, search]);
  const setField = (key, value) => setDraft((draft) => ({ ...draft, [key]: value }));
  const save = async () => { const validation = validateItem(draft, type); if (!validation.valid) return toast({ title: `Complete: ${validation.missing.join(', ')}`, variant: 'destructive' }); const payload = { ...draft, user_id: user.id, tags: typeof draft.tags === 'string' ? draft.tags.split(',').map((item) => item.trim()).filter(Boolean) : draft.tags }; if (editing?.id) { const updated = await Entity.update(editing.id, payload); setItems((items) => items.map((item) => item.id === editing.id ? { ...item, ...updated } : item)); } else { const created = await Entity.create(payload); setItems((items) => [created, ...items]); } setOpen(false); setEditing(null); setDraft(makeBlank()); toast({ title: `${singular} saved` }); };
  const generate = async () => { setBusy(true); try { const result = await firebaseApi.integrations.Core.InvokeLLM({ prompt: `Generate ${ai.count} CELE ${isFormula ? 'formulas' : 'multiple choice questions'} for Subject: ${ai.subject}; Topic: ${ai.topic}; Difficulty: ${ai.difficulty}. Return JSON {items:[...]}.`, response_json_schema: { type: 'object', properties: { items: { type: 'array', items: { type: 'object' } } } } }); setPreview((result.items || []).map((item) => normalizeItem(item, type, 'ai'))); setMode('review'); } catch (error) { toast({ title: error.message || 'AI generation failed', variant: 'destructive' }); } finally { setBusy(false); } };
  const savePreview = async () => { const invalid = preview.filter((item) => !validateItem(item, type).valid); if (invalid.length) return toast({ title: `${invalid.length} item(s) need required fields`, variant: 'destructive' }); setBusy(true); try { const created = await Entity.bulkCreate(preview.map((item) => ({ ...item, user_id: user.id }))); setItems((items) => [...created, ...items]); setPreview([]); setMode('manual'); toast({ title: `${created.length} ${singular.toLowerCase()}s saved` }); } finally { setBusy(false); } };
  const fieldInput = (value, key, change) => ['question', 'formula', 'description', 'explanation', 'solution', 'exampleProblem'].includes(key) ? <Textarea value={value || ''} onChange={(event) => change(event.target.value)} /> : <Input value={key === 'choices' ? (value || []).join(' | ') : Array.isArray(value) ? value.join(' | ') : value || ''} onChange={(event) => change(key === 'choices' ? event.target.value.split('|').map((item) => item.trim()).filter(Boolean) : event.target.value)} />;
  return <div className="space-y-5"><div className="flex flex-wrap gap-3 justify-between"><div><h1 className="text-2xl font-bold">{isFormula ? 'Formula Library' : 'Question Bank'}</h1><p className="text-sm text-muted-foreground">Create manually, generate with AI, or import spreadsheets locally.</p></div><div className="flex gap-2"><ImportExport user={user} cards={items} type={type} onImported={(created) => setItems((items) => [...created, ...items])} /><Button onClick={() => { setDraft(makeBlank()); setEditing(null); setOpen(true); }}><Plus className="w-4 h-4 mr-1" /> Manual input</Button></div></div><Tabs value={mode} onValueChange={setMode}><TabsList className="grid grid-cols-2 w-full"><TabsTrigger value="manual">Library</TabsTrigger><TabsTrigger value="ai">AI generated</TabsTrigger></TabsList>{mode === 'manual' && <div className="space-y-4 mt-4"><div className="relative"><Search className="absolute w-4 h-4 left-3 top-3 text-muted-foreground" /><Input className="pl-9" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search subject, topic, tags, or content" /></div><div className="grid gap-3 md:grid-cols-2">{filtered.map((item) => <article key={item.id} className="glass-card p-4"><div className="flex gap-2 justify-between"><div><h2 className="font-semibold">{item.name || item.question}</h2><p className="text-xs text-muted-foreground">{item.subject} · {item.topic} · {item.sourceType || 'manual'}</p></div><div className="flex"><Button variant="ghost" size="icon" onClick={() => { setDraft({ ...item, tags: Array.isArray(item.tags) ? item.tags.join(', ') : item.tags }); setEditing(item); setOpen(true); }}><Pencil className="w-4 h-4" /></Button><Button variant="ghost" size="icon" onClick={async () => { await Entity.delete(item.id); setItems((items) => items.filter((entry) => entry.id !== item.id)); }}><Trash2 className="w-4 h-4" /></Button></div></div><p className="text-sm mt-2 whitespace-pre-wrap">{item.formula || item.explanation}</p></article>)}</div>{!filtered.length && <p className="py-10 text-center text-muted-foreground">No {singular.toLowerCase()}s yet.</p>}</div>}{mode === 'ai' && <div className="mt-4 glass-card p-5 space-y-3"><Input placeholder="Subject" value={ai.subject} onChange={(event) => setAi((value) => ({ ...value, subject: event.target.value }))} /><Input placeholder="Topic" value={ai.topic} onChange={(event) => setAi((value) => ({ ...value, topic: event.target.value }))} /><Button onClick={generate} disabled={busy || !ai.subject || !ai.topic}>{busy ? <Loader2 className="w-4 h-4 animate-spin mr-2" /> : <Sparkles className="w-4 h-4 mr-2" />}Generate for review</Button></div>}{mode === 'review' && <div className="mt-4 space-y-3"><div className="flex justify-between"><p className="font-medium">Review generated items ({preview.length})</p><Button onClick={savePreview} disabled={busy || !preview.length}>Save validated items</Button></div>{preview.map((item, index) => <div className="glass-card p-4 space-y-2" key={index}>{fields.map(([key, label]) => <div key={key}><Label>{label}</Label>{fieldInput(item[key], key, (value) => setPreview((items) => items.map((entry, i) => i === index ? { ...entry, [key]: value } : entry)))}</div>)}</div>)}</div>}</Tabs><Dialog open={open} onOpenChange={setOpen}><DialogContent className="max-w-2xl max-h-[90vh] overflow-y-auto"><DialogHeader><DialogTitle>{editing ? `Edit ${singular}` : `New ${singular}`}</DialogTitle></DialogHeader><div className="space-y-3">{fields.map(([key, label]) => <div key={key}><Label>{label}</Label>{fieldInput(draft[key], key, (value) => setField(key, value))}</div>)}<Button onClick={save} className="w-full">Save {singular}</Button></div></DialogContent></Dialog></div>;
}
